---
import { Image } from "astro:assets";

const { name, date, location, cover, link } = Astro.props;
const formattedDate = new Date(date).toLocaleDateString("en-US", {
  year: "numeric",
  month: "short",
  day: "numeric",
});
---

<a
  href={`/events/${link}`}
  class="group overflow-hidden cursor-pointer transition-all duration-200 hover:scale-[1.005] block break-inside-avoid mb-4 bg-card shadow-md"
>
  <div class="relative">
    {
      cover && (
        <Image
          src={cover}
          alt={name}
          class="w-full h-auto object-contain bg-gray-100"
          format="webp"
          loading="lazy"
          quality={80}
        />
      )
    }
  </div>

  <div class="p-4 space-y-2">
    <div class="flex items-center gap-2 text-sm text-muted-foreground">
      <span>{formattedDate}</span>
      {location && (
        <>
          <span>•</span>
          <span>{location}</span>
        </>
      )}
    </div>

    <h2 class="text-xl font-bold leading-tight transition-colors group-hover:text-primary">
      {name}
    </h2>
  </div>
</a>
